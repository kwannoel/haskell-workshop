-- | Defining basic functions to work with

module Functions where

---------------------
-- Basic functions --
---------------------

-- Identity
id :: a -> a
id = undefined

-- Addition
add :: Num a => a -> a
add = undefined

-- Safe head of list
safeHead :: [a] -> Maybe a
safeHead = undefined

-- Safe tail of list
safeTail :: [a] -> Maybe [a]
safeTail = undefined

----------------------------
-- Higher order functions --
----------------------------
